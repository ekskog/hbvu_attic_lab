apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gonic-data-pvc
  namespace: gonic
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: gonic-data-pv
# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: gonic
  namespace: gonic
  annotations:
    metallb.universe.tf/address-pool: cluster-pool
spec:
  type: LoadBalancer
  selector:
    app: gonic
  ports:
    - name: http
      port: 4747
      targetPort: 4747
      
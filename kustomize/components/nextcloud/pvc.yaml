kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nextcloud-pvc
  namespace: nextcloud
  annotations:
    volume.beta.kubernetes.io/storage-class: "nfs-client"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
